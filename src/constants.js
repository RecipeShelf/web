/**
 * @typedef {String} SpiceLevel
 */

/**
 * @typedef {String} Decorator
 */

/**
 * @enum {Decorator} Decorator
 */
const Decorator = {
  Heading: "Heading",
  Text: "Text"
};

/**
 * @enum {SpiceLevel}
 */
const SpiceLevel = {
  Sweet: "Sweet",
  Mild: "Mild",
  Medium: "Medium",
  Hot: "Hot"
};

export { Decorator, SpiceLevel };
